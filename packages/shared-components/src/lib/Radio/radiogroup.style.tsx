import { RadioGroup } from '@chakra-ui/react';
import styled from 'styled-components';

import { StyledRadioGroupPropTypes } from './radiogroup.types';

export const StyledRadioGroup = styled(RadioGroup)<StyledRadioGroupPropTypes>``;
