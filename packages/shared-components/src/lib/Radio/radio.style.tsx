import { Radio } from '@chakra-ui/react';
import styled from 'styled-components';

import { StyledRadioPropTypes } from './radio.types';

export const StyledRadio = styled(Radio)<StyledRadioPropTypes>``;
