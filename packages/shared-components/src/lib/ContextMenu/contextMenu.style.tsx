import { Menu as CMenu } from '@chakra-ui/react';
import styled from 'styled-components';

import { StyledContextMenuPropTypes } from './contextMenu.types';

export const StyledContextMenu = styled(CMenu)<StyledContextMenuPropTypes>``;
