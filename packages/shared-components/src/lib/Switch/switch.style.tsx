import { Switch } from '@chakra-ui/react';
import styled from 'styled-components';

import { StyledSwitchPropTypes } from './switch.types';

export const StyledSwitch = styled(Switch)<StyledSwitchPropTypes>``;
