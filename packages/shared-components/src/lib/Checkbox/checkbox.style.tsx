import { Checkbox } from '@chakra-ui/react';
import styled from 'styled-components';

import { StyledCheckboxPropTypes } from './checkbox.types';

export const StyledCheckbox = styled(Checkbox)<StyledCheckboxPropTypes>``;
