{
  "private": true,
  "name": "@app/app-generator-processor",
  "version": "0.0.0",
  "scripts": {
    "build": "esbuild ./src/reportProgress.ts ./src/reportResult.ts --bundle --minify --sourcemap --platform=node --target=node18 --outdir=dist",
    "lint": "eslint ."
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "pg": "^8.11.3",
    "ts-dotenv": "^0.9.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/node": "18.14.2",
    "esbuild": "^0.19.8"
  }
}
