import styled from 'styled-components';

export const StyledEditUserProjectAccess = styled.div`
  background: ${({ theme }) => theme.colors.current.surface2};

  .al-page-header {
    justify-content: flex-end;
  }

  .al-headline {
    margin-bottom: 0;
  }
`;
